<Window x:Class="database.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:database"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="dgFilmy"
                  Grid.Column="0"
                  Margin="0,0,10,0"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  SelectionChanged="dgFilmy_SelectionChanged">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Název" Binding="{Binding Nazev}" Width="*"/>
                <DataGridTextColumn Header="Režisér" Binding="{Binding Reziser}" Width="*"/>
                <DataGridTextColumn Header="Rok" Binding="{Binding Rok}" Width="80"/>
                <DataGridTextColumn Header="Žánr" Binding="{Binding Zanr}" Width="120"/>
                <DataGridTextColumn Header="Ks" Binding="{Binding Pocet}" Width="60"/>
                <DataGridCheckBoxColumn Header="Dostupný" Binding="{Binding Dostupny}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Column="1">
            <TextBlock Text="Název"/>
            <TextBox x:Name="tbNazev"/>     
            
            <TextBlock Text="Režisér"/>
            <TextBox x:Name="tbReziser"/>
            
            <TextBlock Text="Rok"/>
            <TextBox x:Name="tbRok"/>
            
            <TextBlock Text="Žánr"/>
            <TextBox x:Name="tbZanr"/>
            
            <TextBlock Text="Počet"/>
            <TextBox x:Name="tbPocet"/>
            
            <CheckBox x:Name="cbDostupny" Content="Dostupný"/>
            
            <Button Content="Přidat / Upravit" Click="BtnUlozit_Click"/>
            <Button Content="Smazat" Click="BtnSmazat_Click" Margin="0, 20, 0 ,0" RenderTransformOrigin="0.523,2.678"/>
            
            <TextBlock Text="Vyhledávání"/>
            <TextBox x:Name="tbFilter" TextChanged="tbFilter_TextChanged"/>
        </StackPanel>
    </Grid>
</Window>
